[build]
publish = "public"
command = "hugo --gc --minify"

[build.environment]
HUGO_VERSION = "0.157.0"
NODE_VERSION = "20"

[context.production.environment]
HUGO_ENV = "production"
HUGO_ENABLEGITINFO = "true"
